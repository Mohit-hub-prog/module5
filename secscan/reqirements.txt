nvdlib>=0.5.0



For the remote execution, you need a remote connection to your local machine




If virtual environment needed, run the following commands in same directory:
       pip install --upgrade pip --break-system-packages
       pip install nvdlib --break-system-packages
